"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.publicConfig = exports.privateConfig = exports.default = void 0;

var _destr = _interopRequireDefault(require("destr"));

var _defu = _interopRequireDefault(require("defu"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const _runtimeConfig = process.env.RUNTIME_CONFIG;

for (const type of ["private", "public"]) {
  for (const key in _runtimeConfig[type]) {
    _runtimeConfig[type][key] = (0, _destr.default)(process.env[key] || _runtimeConfig[type][key]);
  }
}

const privateConfig = deepFreeze((0, _defu.default)(_runtimeConfig.private, _runtimeConfig.public));
exports.privateConfig = privateConfig;
const publicConfig = deepFreeze(_runtimeConfig.public);
exports.publicConfig = publicConfig;
var _default = privateConfig;
module.exports = _default;

function deepFreeze(object) {
  const propNames = Object.getOwnPropertyNames(object);

  for (const name of propNames) {
    const value = object[name];

    if (value && typeof value === "object") {
      deepFreeze(value);
    }
  }

  return Object.freeze(object);
}